# 計画

## 将来実装したい機能
- ブルートフォース対策が何も無いので、前回ログインがn日以上前ならログインを拒否。前回ログイン日時を更新しつつも、ログイン拒否のデータファイルを消さない限りログインを拒否し続けるようにしたい。前回ログイン日時をそのファイルに記録するのもいい。そのファイルの読み書きにはロックが必要。

## 認証運用方針
- 初回起動時にセットアップ画面を表示し、管理パスワードを設定して `.vp_auth.php` へ保存する。
- 認証情報は `.vp_auth.php` のみを参照し、`vp.php` 内に固定ハッシュは持たない。
